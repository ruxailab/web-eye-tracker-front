import Vue from 'vue'
import Vuex from 'vuex'

import session from '@/store/session.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    session
  }
})
